//old example
var PI = 3.14

//ES2015

const PI = 3.14;

//Quiz
//1. Var is scoped within a function, let is scoped within a codeblock. Let cannot be redeclared.
//2. Var is scoped within a function and can be reassigned/redeclared, const is scoped within a codeblock and cannot be reassigned.
//3.  Const cannot be reassigned and let can be.
//4. Variable hoisting is basically that the browser reads the variable name (when using var) before running the code in our script. So if you call a variable before it is initialized, it will just say undefined instead of a reference error.
